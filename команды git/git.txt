
Конечно! Вот некоторые **часто используемые команды Git**, которые помогут вам в разработке:

1. **`git init`**: Инициализирует проект как репозиторий Git.
2. **`git clone`**: Клонирует репозиторий в новую директорию. Пример: `git clone <URL>`.
3. **`git remote add`**: Добавляет или подключается к удаленному репозиторию. Пример: `git remote add origin <URL>`.
4. **`git remote -v`**: Просмотр подключенных удаленных репозиториев.
5. **`git status`**: Просмотр статуса файлов в вашем локальном репозитории.
6. **`git add`**: Переносит новые и измененные файлы в раздел проиндексированных файлов. Пример: `git add index.html style.css`.
7. **`git reset`**: Мягкая или жесткая отмена изменений. Пример: `git reset --hard HEAD^`.
8. **`git commit`**: Закрепляет промежуточные результаты. Пример: `git commit -m "added navigation bar"`.
9. **`git push`**: Отправляет закоммиченные файлы в удаленный репозиторий. Пример: `git push -u origin master`.
10. **`git fetch`**: Получает изменения из удаленного репозитория.

Эти команды помогут вам эффективно управлять вашими Git-репозиториями! Если у вас есть еще вопросы, не стесняйтесь спрашивать. 😊
